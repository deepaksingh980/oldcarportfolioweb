export const metadata = {
  title: "Cars - Old Car Portfolio",
  description: "Browse our vintage car collection with search and filters.",
};

export default function CarsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
